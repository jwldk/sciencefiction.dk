extends ../layouts/default

block content
  h2= publication.title
  .row
    .col-md-8
      .article-content
        != publication.content
    .col-md-4
      img.img-responsive(src=publication.image.url)
